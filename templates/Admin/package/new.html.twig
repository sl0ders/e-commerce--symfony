{% extends 'base.html.twig' %}
{% trans_default_domain "NegasProjectTrans" %}
{% block body %}
    <div class="container">
        <div class="row text-center">
            <div class="col-md-10 m-auto border shadow p-5 rgba-blue-grey-light">
                <h1>{{ "package.title.new"| trans }}</h1>
                <div class="form_content">
                    {{ include('Admin/package/_form.html.twig') }}
                </div>
                    <a href="{{ path('admin_package_index') }}" class="btn btn-primary float-left">{{ "button.return" |trans }}</a>
            </div>
        </div>
    </div>
{% endblock %}

