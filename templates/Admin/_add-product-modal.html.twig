{% trans_default_domain "NegasProjectTrans" %}
<!-- Modal: modalCart -->
<div class="modal fade" id="modalAddProduct" tabindex="-1" role="dialog" aria-labelledby="AddProductModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content datatable formulary">
            <!--Header-->
            <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel">{{ "product.title.new"| trans }}</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <!--Body-->
            <div class="modal-body col-md-12">
                    <div class="datatable_content">
                        {{ include('Admin/product/_form.html.twig') }}
                    </div>
            </div>
        </div>
    </div>
</div>
<!-- Modal: modalCart -->
